# 🎬 Извлечение 30 кадров из видео

## 🎯 Описание
Извлекает **30 кадров** из каждого видео с **равномерным распределением** по времени:
- Видео делится на 30 равных сегментов
- Берется первый кадр из каждого сегмента
- Кадры сохраняются в облако UTair

## 🚀 Быстрый запуск

### ⚡ Самый простой способ:
```
Дважды кликните: ИЗВЛЕЧЬ_30_КАДРОВ.bat
```

### 🐍 Python команды:
```bash
# Быстрое извлечение (обновленный скрипт)
python extract_frames.py

# Продвинутое извлечение с облаком
python extract_30_frames.py

# С настройками
python extract_30_frames.py --frames 30 --format jpg --quality 95
```

## 📊 Алгоритм распределения

### Как работает равномерное распределение:
```
Видео длительностью 60 секунд, 1800 кадров:
├─ Сегмент 1: кадры 0-60     → берем кадр 0
├─ Сегмент 2: кадры 60-120   → берем кадр 60  
├─ Сегмент 3: кадры 120-180  → берем кадр 120
│  ...
└─ Сегмент 30: кадры 1740-1800 → берем кадр 1740

Результат: 30 кадров равномерно по всему видео
```

### Преимущества:
- ✅ Покрытие всего видео от начала до конца
- ✅ Одинаковые временные интервалы между кадрами
- ✅ Оптимально для машинного обучения
- ✅ Не пропускает важные сцены в начале/конце

## 💾 Сохранение кадров

### 🌐 В облако UTair (рекомендуемый):
```
Структура в облаке:
/TST_project/
├── Videos/           # Исходные видео
└── Extracted_Frames/ # Извлеченные кадры
    ├── Видео1/       # Подпапка для каждого видео
    │   ├── кадр_01_pos_000000.jpg
    │   ├── кадр_02_pos_000060.jpg
    │   └── ...
    └── Видео2/
        └── ...
```

### 📁 Локально (fallback):
```
TST_Project/
├── Videos/          # Символическая ссылка на облако
├── Images/          # Локальные кадры (если облако недоступно)
└── Extracted_Frames/ # Альтернативная папка
```

## ⚙️ Настройки

### 📝 Параметры по умолчанию:
- **Кадров на видео:** 30
- **Формат:** JPG
- **Качество:** 95%
- **Подпапки:** Да (для каждого видео)
- **Пропуск готовых:** Да

### 🔧 Изменение настроек:
```bash
# Больше кадров
python extract_30_frames.py --frames 50

# PNG формат
python extract_30_frames.py --format png

# Без подпапок
python extract_30_frames.py --no-subfolders

# Переизвлечь все заново
python extract_30_frames.py --force
```

## 🔍 Проверка результатов

### 📊 Статистика:
```bash
# Проверка синхронизации
python check_cloud_sync.py

# Быстрая проверка
python check_cloud_sync.py --quick

# Интерактивный режим
python check_cloud_sync.py --interactive
```

### 🌐 В облаке:
**Ссылка:** https://cloud.utair.ru/apps/files/?dir=/TST_project

## 📋 Имена файлов

### 🏷️ Формат имен:
```
{video_name}_frame_{номер}_pos_{позиция}.jpg

Примеры:
• 1_секция_задняя_правая_20250723_101500_frame_01_pos_000000.jpg
• 1_секция_задняя_правая_20250723_101500_frame_02_pos_002400.jpg
• 1_секция_задняя_правая_20250723_101500_frame_30_pos_072000.jpg
```

### 📊 Информация в имени:
- `frame_01` - номер кадра (1-30)
- `pos_000000` - позиция в исходном видео
- Позиция помогает понять временную метку

## 🔄 Рабочий процесс

### 1️⃣ Подготовка:
```bash
# Проверить доступ к видео
python check_cloud_sync.py --videos-only

# При необходимости настроить облако
НАСТРОИТЬ_ОБЛАКО.bat
```

### 2️⃣ Извлечение:
```bash
# Запустить извлечение
ИЗВЛЕЧЬ_30_КАДРОВ.bat
# или
python extract_30_frames.py
```

### 3️⃣ Проверка:
```bash
# Проверить результаты
python check_cloud_sync.py

# Посмотреть статистику
ls Extracted_Frames/*/  # Linux/Mac
dir Extracted_Frames\*\ # Windows
```

### 4️⃣ Следующие шаги:
```bash
# Подготовка для Roboflow
python ПОЛНЫЙ_PIPELINE.py --prepare-roboflow

# Веб-интерфейс
streamlit run app.py
```

## 🚨 Решение проблем

### ❌ Папка Videos недоступна:
```bash
# Настроить облако
НАСТРОИТЬ_ОБЛАКО.bat

# Проверить подключение
python check_cloud_sync.py
```

### 🔄 Медленное извлечение:
- Используйте SSD для временных файлов
- Закройте ненужные программы
- Проверьте скорость облачного соединения

### 📁 Кадры не сохраняются в облако:
```bash
# Проверить права доступа
# Создать локальную папку как fallback
mkdir Extracted_Frames

# Использовать локальное сохранение
python extract_30_frames.py --output "Extracted_Frames"
```

### 🎬 Ошибки при чтении видео:
- Проверьте формат видеофайла
- Убедитесь, что файл не поврежден
- Попробуйте переконвертировать видео

## 📈 Оптимизация

### ⚡ Для ускорения:
```bash
# Меньше кадров для тестирования
python extract_30_frames.py --frames 10

# Сжатие изображений
python extract_30_frames.py --quality 80

# Пропуск уже готовых
python extract_30_frames.py  # (по умолчанию включено)
```

### 💾 Для экономии места:
```bash
# PNG формат (без потерь, но больше размер)
python extract_30_frames.py --format png

# Высокое качество JPEG
python extract_30_frames.py --quality 95
```

## 📊 Статистика

### 📈 Ожидаемые объемы:
- **30 кадров × 8 видео = 240 кадров**
- **Размер кадра: ~200-500 KB**
- **Общий объем: ~50-120 MB**

### ⏱️ Время обработки:
- **1 видео (30 кадров): ~10-30 секунд**
- **8 видео: ~2-5 минут**
- Зависит от длительности видео и скорости диска

## 💡 Рекомендации

### ✅ Лучшие практики:
1. **Проверяйте облако** перед извлечением
2. **Используйте равномерное распределение** для ML
3. **Сохраняйте в облако** для синхронизации
4. **Проверяйте результаты** после извлечения

### 🎯 Для Roboflow:
- 30 кадров оптимально для разметки
- Равномерное распределение покрывает все сцены
- JPG формат подходит для большинства задач
- Качество 95% сохраняет детали

---

**🚀 Быстрый старт:** `ИЗВЛЕЧЬ_30_КАДРОВ.bat`  
**🌐 Проверка облака:** `python check_cloud_sync.py`  
**📚 Документация:** `README_ROBOFLOW_PIPELINE.md`